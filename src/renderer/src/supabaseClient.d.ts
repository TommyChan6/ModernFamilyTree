// Type declarations for supabaseClient.js (kept as JS so it stays readable
// next to the .env it documents). Gives TS callers the real client type.
import type { SupabaseClient } from '@supabase/supabase-js'

export declare const supabase: SupabaseClient
export default supabase
